# 하얌이 / HAYAM — Live2D 리깅 커미션 페이지

공개 주소: **https://n-ightmar-e.github.io/artmug-rigging-page/**

`main` 브랜치에 푸시하면 자동으로 다시 배포됩니다 (반영까지 1~2분).

## 프로필 사진 바꾸기

`artmug-assets/profile.png` 를 같은 이름의 새 이미지로 덮어쓰면 됩니다. 코드 수정은 필요 없습니다.

- 권장: 정사각형, 400×400px 이상
- 페이지에서는 120px 원각 사각형으로 잘려(`object-fit: cover`) 표시됩니다

## 작가명 바꾸기

`index.html`에서 `하얌이` 와 `HAYAM` 을 검색해 교체하세요 (`<title>`, hero 제목, 프로필 사진 alt).

같은 안내가 `index.html` 맨 아래 주석에도 있습니다.
